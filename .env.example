# Database (production)
DATABASE_URL=postgres://user:password@localhost:5432/tabdevs_pl_production

# Resend (email)
RESEND_API_KEY=re_xxxxxxxxxxxx

# Rails
RAILS_ENV=development
SECRET_KEY_BASE=generate_with_bin_rails_secret
RAILS_MAX_THREADS=5

# Application (production)
APP_NAME=your-app-name
APP_HOST=your-domain.com
APP_HOSTS=your-domain.com,www.your-domain.com

# Optional: Redis for caching (if not using SolidCache)
# REDIS_URL=redis://localhost:6379/0
